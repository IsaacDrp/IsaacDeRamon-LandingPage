version: '3.8'

services:
  portfolio:
    build: .
    container_name: isaac-portfolio
    restart: unless-stopped
    environment:
      - HOST=0.0.0.0 
    networks:
      - synectura-net

networks:
  synectura-net:
    external: true